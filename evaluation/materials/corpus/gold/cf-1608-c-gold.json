{"algorithm":"Create a directed beat relation, compressed to adjacent stronger-to-weaker edges in each sorted map ranking. Reverse these edges and traverse from the strongest player on the first map. Exactly the visited players can win.","code":"#include <bits/stdc++.h>\nusing namespace std;\nint main() {\n    ios::sync_with_stdio(false); cin.tie(nullptr);\n    int t; cin >> t;\n    while (t--) {\n        int n; cin >> n;\n        vector<int> a(n), b(n), by_a(n), by_b(n);\n        for (int &x : a) cin >> x;\n        for (int &x : b) cin >> x;\n        iota(by_a.begin(), by_a.end(), 0);\n        iota(by_b.begin(), by_b.end(), 0);\n        sort(by_a.begin(), by_a.end(), [&](int u, int v) { return a[u] < a[v]; });\n        sort(by_b.begin(), by_b.end(), [&](int u, int v) { return b[u] < b[v]; });\n        vector<vector<int>> rev(n);\n        for (int i = 1; i < n; ++i) {\n            rev[by_a[i - 1]].push_back(by_a[i]);\n            rev[by_b[i - 1]].push_back(by_b[i]);\n        }\n        string answer(n, '0');\n        queue<int> todo;\n        todo.push(by_a.back()); answer[by_a.back()] = '1';\n        while (!todo.empty()) {\n            int u = todo.front(); todo.pop();\n            for (int v : rev[u]) if (answer[v] == '0') {\n                answer[v] = '1'; todo.push(v);\n            }\n        }\n        cout << answer << '\\n';\n    }\n}\n","correctness_argument":"The adjacent ranking chains preserve reachability of the full beat graph: any stronger player reaches every weaker player through its map's chain. A player can win a tournament exactly when it reaches all players in the beat graph. Necessity follows by tracing the elimination tree; sufficiency follows by taking an outward spanning tree and eliminating descendants before their parents. The strongest first-map player reaches everyone, so a player reaches everyone precisely when it reaches that player. Reverse traversal computes exactly this predecessor set.","edge_cases":["A single player wins automatically.","Identical map orders leave only their maximum eligible."],"language":"cpp17","problem_id":"cf-1608-c","problem_understanding":"Model each permitted match winner-to-loser relation as a directed edge.","schema_version":"1.2","space_complexity":"O(n)","steps":[{"claim":"Model each permitted match winner-to-loser relation as a directed edge.","depends_on":[],"rationale":"Model each permitted match winner-to-loser relation as a directed edge.","schema_version":"1.2","stage":"problem_understanding","status":"correct","step_id":"s1","step_number":1},{"claim":"Create a directed beat relation, compressed to adjacent stronger-to-weaker edges in each sorted map ranking. Reverse these edges and traverse from the strongest player on the first map. Exactly the visited players can win.","depends_on":["s1"],"rationale":"Create a directed beat relation, compressed to adjacent stronger-to-weaker edges in each sorted map ranking. Reverse these edges and traverse from the strongest player on the first map. Exactly the visited players can win.","schema_version":"1.2","stage":"algorithm_design","status":"correct","step_id":"s2","step_number":2},{"claim":"The adjacent ranking chains preserve reachability of the full beat graph: any stronger player reaches every weaker player through its map's chain. A player can win a tournament exactly when it reaches all players in the beat graph. Necessity follows by tracing the elimination tree; sufficiency follows by taking an outward spanning tree and eliminating descendants before their parents. The strongest first-map player reaches everyone, so a player reaches everyone precisely when it reaches that player. Reverse traversal computes exactly this predecessor set.","depends_on":["s2"],"rationale":"The adjacent ranking chains preserve reachability of the full beat graph: any stronger player reaches every weaker player through its map's chain. A player can win a tournament exactly when it reaches all players in the beat graph. Necessity follows by tracing the elimination tree; sufficiency follows by taking an outward spanning tree and eliminating descendants before their parents. The strongest first-map player reaches everyone, so a player reaches everyone precisely when it reaches that player. Reverse traversal computes exactly this predecessor set.","schema_version":"1.2","stage":"correctness_argument","status":"correct","step_id":"s3","step_number":3},{"claim":"Time: O(n log n) per test case; space: O(n).","depends_on":["s3"],"rationale":"Time: O(n log n) per test case; space: O(n).","schema_version":"1.2","stage":"complexity_analysis","status":"correct","step_id":"s4","step_number":4},{"claim":"Implementation follows these obligations: Model each permitted match winner-to-loser relation as a directed edge. Replace each map's dense relation with its adjacent ranking chain. Prove tournament winners are exactly vertices that reach all others. Reduce universal reachability to reaching the strongest first-map player. Traverse reversed chain edges and output the visited set in original order.","depends_on":["s4"],"rationale":"Implementation follows these obligations: Model each permitted match winner-to-loser relation as a directed edge. Replace each map's dense relation with its adjacent ranking chain. Prove tournament winners are exactly vertices that reach all others. Reduce universal reachability to reaching the strongest first-map player. Traverse reversed chain edges and output the visited set in original order.","schema_version":"1.2","stage":"implementation","status":"correct","step_id":"s5","step_number":5},{"claim":"Check boundary cases: A single player wins automatically. Identical map orders leave only their maximum eligible.","depends_on":["s5"],"rationale":"Check boundary cases: A single player wins automatically. Identical map orders leave only their maximum eligible.","schema_version":"1.2","stage":"edge_cases","status":"correct","step_id":"s6","step_number":6}],"time_complexity":"O(n log n) per test case","trace_id":"cf-1608-c-gold"}