{"algorithm":"Sort the array and leave the smallest n-2k elements unpaired. Split the largest 2k elements into sorted halves of length k and pair corresponding positions across the halves. Each pair contributes one if equal and zero otherwise; add the retained prefix sum.","code":"#include <bits/stdc++.h>\nusing namespace std;\nint main() {\n    ios::sync_with_stdio(false); cin.tie(nullptr);\n    int t; cin >> t;\n    while (t--) {\n        int n, k; cin >> n >> k;\n        vector<int> a(n);\n        for (int &x : a) cin >> x;\n        sort(a.begin(), a.end());\n        long long answer = 0;\n        int keep = n - 2 * k;\n        for (int i = 0; i < keep; ++i) answer += a[i];\n        for (int i = 0; i < k; ++i)\n            answer += (a[keep + i] == a[keep + k + i]);\n        cout << answer << '\\n';\n    }\n}\n","correctness_argument":"Orient every pair with its smaller value as numerator, making its cost zero or one. If a smaller used value y and a larger retained value x are exchanged, the retained sum drops by x-y while pair cost can rise by at most one. Distinct integers have x-y>=1, and equal values change nothing, so keeping the smallest elements is optimal. Among 2k selected values, a frequency f>k forces at least f-k equal pairs. Pairing sorted positions distance k apart has exactly max(0,f-k) equal pairs for each value's contiguous run. At most one frequency exceeds k, so this reaches the unavoidable lower bound.","edge_cases":["k=0 returns the entire array sum.","n=2k leaves no retained values.","All equal values force k unit-cost pairs."],"language":"cpp17","problem_id":"cf-1618-d","problem_understanding":"Orient each division so that pair cost is zero for unequal values and one for equal values.","schema_version":"1.2","space_complexity":"O(n)","steps":[{"claim":"Orient each division so that pair cost is zero for unequal values and one for equal values.","depends_on":[],"rationale":"Orient each division so that pair cost is zero for unequal values and one for equal values.","schema_version":"1.2","stage":"problem_understanding","status":"correct","step_id":"s1","step_number":1},{"claim":"Sort the array and leave the smallest n-2k elements unpaired. Split the largest 2k elements into sorted halves of length k and pair corresponding positions across the halves. Each pair contributes one if equal and zero otherwise; add the retained prefix sum.","depends_on":["s1"],"rationale":"Sort the array and leave the smallest n-2k elements unpaired. Split the largest 2k elements into sorted halves of length k and pair corresponding positions across the halves. Each pair contributes one if equal and zero otherwise; add the retained prefix sum.","schema_version":"1.2","stage":"algorithm_design","status":"correct","step_id":"s2","step_number":2},{"claim":"Orient every pair with its smaller value as numerator, making its cost zero or one. If a smaller used value y and a larger retained value x are exchanged, the retained sum drops by x-y while pair cost can rise by at most one. Distinct integers have x-y>=1, and equal values change nothing, so keeping the smallest elements is optimal. Among 2k selected values, a frequency f>k forces at least f-k equal pairs. Pairing sorted positions distance k apart has exactly max(0,f-k) equal pairs for each value's contiguous run. At most one frequency exceeds k, so this reaches the unavoidable lower bound.","depends_on":["s2"],"rationale":"Orient every pair with its smaller value as numerator, making its cost zero or one. If a smaller used value y and a larger retained value x are exchanged, the retained sum drops by x-y while pair cost can rise by at most one. Distinct integers have x-y>=1, and equal values change nothing, so keeping the smallest elements is optimal. Among 2k selected values, a frequency f>k forces at least f-k equal pairs. Pairing sorted positions distance k apart has exactly max(0,f-k) equal pairs for each value's contiguous run. At most one frequency exceeds k, so this reaches the unavoidable lower bound.","schema_version":"1.2","stage":"correctness_argument","status":"correct","step_id":"s3","step_number":3},{"claim":"Time: O(n log n) per test case; space: O(n).","depends_on":["s3"],"rationale":"Time: O(n log n) per test case; space: O(n).","schema_version":"1.2","stage":"complexity_analysis","status":"correct","step_id":"s4","step_number":4},{"claim":"Implementation follows these obligations: Orient each division so that pair cost is zero for unequal values and one for equal values. Use an exchange argument to retain the smallest n-2k values. Bound unavoidable equal pairs by any value frequency exceeding k. Pair sorted selected positions k apart to attain that bound. Add the retained prefix sum and the equal-pair count.","depends_on":["s4"],"rationale":"Implementation follows these obligations: Orient each division so that pair cost is zero for unequal values and one for equal values. Use an exchange argument to retain the smallest n-2k values. Bound unavoidable equal pairs by any value frequency exceeding k. Pair sorted selected positions k apart to attain that bound. Add the retained prefix sum and the equal-pair count.","schema_version":"1.2","stage":"implementation","status":"correct","step_id":"s5","step_number":5},{"claim":"Check boundary cases: k=0 returns the entire array sum. n=2k leaves no retained values. All equal values force k unit-cost pairs.","depends_on":["s5"],"rationale":"Check boundary cases: k=0 returns the entire array sum. n=2k leaves no retained values. All equal values force k unit-cost pairs.","schema_version":"1.2","stage":"edge_cases","status":"correct","step_id":"s6","step_number":6}],"time_complexity":"O(n log n) per test case","trace_id":"cf-1618-d-gold"}