{"algorithm":"Compress the values and maintain frequencies of already processed elements in a Fenwick tree. Inserting the new value at the front adds one inversion for each strictly smaller earlier value, while inserting at the back adds one for each strictly larger value. Add the smaller of these counts, then insert the frequency.","code":"#include <bits/stdc++.h>\nusing namespace std;\nstruct Fenwick {\n    int n; vector<int> bit;\n    explicit Fenwick(int n): n(n), bit(n + 1) {}\n    int sum(int p) {\n        int r = 0;\n        for (; p > 0; p -= p & -p) r += bit[p];\n        return r;\n    }\n    void add(int p) {\n        for (; p <= n; p += p & -p) ++bit[p];\n    }\n};\nint main() {\n    ios::sync_with_stdio(false); cin.tie(nullptr);\n    int t; cin >> t;\n    while (t--) {\n        int n; cin >> n;\n        vector<int> a(n);\n        for (int &x : a) cin >> x;\n        vector<int> values = a;\n        sort(values.begin(), values.end());\n        values.erase(unique(values.begin(), values.end()), values.end());\n        Fenwick fw(values.size());\n        long long answer = 0;\n        for (int i = 0; i < n; ++i) {\n            int rank = lower_bound(values.begin(), values.end(), a[i]) - values.begin() + 1;\n            int smaller = fw.sum(rank - 1);\n            int larger = i - fw.sum(rank);\n            answer += min(smaller, larger);\n            fw.add(rank);\n        }\n        cout << answer << '\\n';\n    }\n}\n","correctness_argument":"A new element is placed entirely before or entirely after the existing deque, so its inversion contribution depends only on the multiset of earlier values, not their arrangement. This choice leaves old inversions unchanged and does not alter the multiset seen by later steps. Therefore each step can independently minimize its contribution. Every inversion is counted exactly when its later-inserted member arrives, so the sum of these independently minimal contributions is globally optimal.","edge_cases":["An all-equal array has zero inversions.","A singleton has zero cost.","Negative values work through coordinate compression."],"language":"cpp17","problem_id":"cf-1579-e2","problem_understanding":"Express the inversion cost of front insertion as the number of smaller earlier values.","schema_version":"1.2","space_complexity":"O(n)","steps":[{"claim":"Express the inversion cost of front insertion as the number of smaller earlier values.","depends_on":[],"rationale":"Express the inversion cost of front insertion as the number of smaller earlier values.","schema_version":"1.2","stage":"problem_understanding","status":"correct","step_id":"s1","step_number":1},{"claim":"Compress the values and maintain frequencies of already processed elements in a Fenwick tree. Inserting the new value at the front adds one inversion for each strictly smaller earlier value, while inserting at the back adds one for each strictly larger value. Add the smaller of these counts, then insert the frequency.","depends_on":["s1"],"rationale":"Compress the values and maintain frequencies of already processed elements in a Fenwick tree. Inserting the new value at the front adds one inversion for each strictly smaller earlier value, while inserting at the back adds one for each strictly larger value. Add the smaller of these counts, then insert the frequency.","schema_version":"1.2","stage":"algorithm_design","status":"correct","step_id":"s2","step_number":2},{"claim":"A new element is placed entirely before or entirely after the existing deque, so its inversion contribution depends only on the multiset of earlier values, not their arrangement. This choice leaves old inversions unchanged and does not alter the multiset seen by later steps. Therefore each step can independently minimize its contribution. Every inversion is counted exactly when its later-inserted member arrives, so the sum of these independently minimal contributions is globally optimal.","depends_on":["s2"],"rationale":"A new element is placed entirely before or entirely after the existing deque, so its inversion contribution depends only on the multiset of earlier values, not their arrangement. This choice leaves old inversions unchanged and does not alter the multiset seen by later steps. Therefore each step can independently minimize its contribution. Every inversion is counted exactly when its later-inserted member arrives, so the sum of these independently minimal contributions is globally optimal.","schema_version":"1.2","stage":"correctness_argument","status":"correct","step_id":"s3","step_number":3},{"claim":"Time: O(n log n) per test case; space: O(n).","depends_on":["s3"],"rationale":"Time: O(n log n) per test case; space: O(n).","schema_version":"1.2","stage":"complexity_analysis","status":"correct","step_id":"s4","step_number":4},{"claim":"Implementation follows these obligations: Express the inversion cost of front insertion as the number of smaller earlier values. Express the cost of back insertion as the number of larger earlier values. Observe that the earlier multiset is independent of all previous placement choices. Query both strict counts with a compressed frequency Fenwick tree. Add their minimum and insert the current value's frequency.","depends_on":["s4"],"rationale":"Implementation follows these obligations: Express the inversion cost of front insertion as the number of smaller earlier values. Express the cost of back insertion as the number of larger earlier values. Observe that the earlier multiset is independent of all previous placement choices. Query both strict counts with a compressed frequency Fenwick tree. Add their minimum and insert the current value's frequency.","schema_version":"1.2","stage":"implementation","status":"correct","step_id":"s5","step_number":5},{"claim":"Check boundary cases: An all-equal array has zero inversions. A singleton has zero cost. Negative values work through coordinate compression.","depends_on":["s5"],"rationale":"Check boundary cases: An all-equal array has zero inversions. A singleton has zero cost. Negative values work through coordinate compression.","schema_version":"1.2","stage":"edge_cases","status":"correct","step_id":"s6","step_number":6}],"time_complexity":"O(n log n) per test case","trace_id":"cf-1579-e2-gold"}