{"algorithm":"Process bit columns from most significant to least significant with counts for AND and XOR prefixes being equal or AND being greater. An odd n has 2^(n-1)+1 equal columns and no greater column; an even n has 2^(n-1)-1 equal columns and one greater column. Once greater, all 2^n columns are allowed.","code":"#include <bits/stdc++.h>\nusing namespace std;\nusing ll = long long;\nconst ll MOD = 1000000007;\nll power(ll a, int e) {\n    ll r = 1;\n    while (e) {\n        if (e & 1) r = r * a % MOD;\n        a = a * a % MOD;\n        e >>= 1;\n    }\n    return r;\n}\nint main() {\n    ios::sync_with_stdio(false); cin.tie(nullptr);\n    int t; cin >> t;\n    while (t--) {\n        int n, k; cin >> n >> k;\n        ll half = power(2, n - 1), all = half * 2 % MOD;\n        ll equal_choices = (half + (n % 2 ? 1 : MOD - 1)) % MOD;\n        ll greater_choices = (n % 2 == 0 ? 1 : 0);\n        ll equal = 1, greater = 0;\n        for (int bit = 0; bit < k; ++bit) {\n            greater = (greater * all + equal * greater_choices) % MOD;\n            equal = equal * equal_choices % MOD;\n        }\n        cout << (equal + greater) % MOD << '\\n';\n    }\n}\n","correctness_argument":"Exactly half of n-bit columns have even parity. The all-one column must be treated separately: it has AND=1 and XOR=n mod 2. Every other column has AND=0. Thus the listed equal and greater multiplicities are exhaustive. Lexicographic comparison of binary representations is decided by the first unequal bit, giving the two-state recurrence. The equal and greater terminal states partition all winning arrays.","edge_cases":["k=0 has exactly the all-zero array.","n=1 makes every array winning."],"language":"cpp17","problem_id":"cf-1557-c","problem_understanding":"Count even-parity bit columns as 2^(n-1).","schema_version":"1.2","space_complexity":"O(1) auxiliary space","steps":[{"claim":"Count even-parity bit columns as 2^(n-1).","depends_on":[],"rationale":"Count even-parity bit columns as 2^(n-1).","schema_version":"1.2","stage":"problem_understanding","status":"correct","step_id":"s1","step_number":1},{"claim":"Process bit columns from most significant to least significant with counts for AND and XOR prefixes being equal or AND being greater. An odd n has 2^(n-1)+1 equal columns and no greater column; an even n has 2^(n-1)-1 equal columns and one greater column. Once greater, all 2^n columns are allowed.","depends_on":["s1"],"rationale":"Process bit columns from most significant to least significant with counts for AND and XOR prefixes being equal or AND being greater. An odd n has 2^(n-1)+1 equal columns and no greater column; an even n has 2^(n-1)-1 equal columns and one greater column. Once greater, all 2^n columns are allowed.","schema_version":"1.2","stage":"algorithm_design","status":"correct","step_id":"s2","step_number":2},{"claim":"Exactly half of n-bit columns have even parity. The all-one column must be treated separately: it has AND=1 and XOR=n mod 2. Every other column has AND=0. Thus the listed equal and greater multiplicities are exhaustive. Lexicographic comparison of binary representations is decided by the first unequal bit, giving the two-state recurrence. The equal and greater terminal states partition all winning arrays.","depends_on":["s2"],"rationale":"Exactly half of n-bit columns have even parity. The all-one column must be treated separately: it has AND=1 and XOR=n mod 2. Every other column has AND=0. Thus the listed equal and greater multiplicities are exhaustive. Lexicographic comparison of binary representations is decided by the first unequal bit, giving the two-state recurrence. The equal and greater terminal states partition all winning arrays.","schema_version":"1.2","stage":"correctness_argument","status":"correct","step_id":"s3","step_number":3},{"claim":"Time: O(log n + k) per test case; space: O(1) auxiliary space.","depends_on":["s3"],"rationale":"Time: O(log n + k) per test case; space: O(1) auxiliary space.","schema_version":"1.2","stage":"complexity_analysis","status":"correct","step_id":"s4","step_number":4},{"claim":"Implementation follows these obligations: Count even-parity bit columns as 2^(n-1). Classify the all-one column according to the parity of n. Maintain equal and greater comparison states from high bits to low bits. Extend a greater state by all 2^n possible columns. Return the sum of equal and greater states modulo 1000000007.","depends_on":["s4"],"rationale":"Implementation follows these obligations: Count even-parity bit columns as 2^(n-1). Classify the all-one column according to the parity of n. Maintain equal and greater comparison states from high bits to low bits. Extend a greater state by all 2^n possible columns. Return the sum of equal and greater states modulo 1000000007.","schema_version":"1.2","stage":"implementation","status":"correct","step_id":"s5","step_number":5},{"claim":"Check boundary cases: k=0 has exactly the all-zero array. n=1 makes every array winning.","depends_on":["s5"],"rationale":"Check boundary cases: k=0 has exactly the all-zero array. n=1 makes every array winning.","schema_version":"1.2","stage":"edge_cases","status":"correct","step_id":"s6","step_number":6}],"time_complexity":"O(log n + k) per test case","trace_id":"cf-1557-c-gold"}